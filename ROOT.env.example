# General
NODE_ENV=development

# RPC endpoints (fill with your testnet endpoints)
DAG_RPC_URL=https://rpc.testnet.your-dag-chain.org
DAG_WS_URL=wss://ws.testnet.your-dag-chain.org

# Indexer DB
POSTGRES_URL=postgresql://postgres:postgres@localhost:5432/dagive

# Optional: relayer/sponsor private key (use GitHub secrets in CI, never commit)
SPONSOR_PRIVATE_KEY=
